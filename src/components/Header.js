import React from 'react';

const Header = () => {
    return (
        <div className="header">
            <span className="font-semibold">Admin Dashboard</span> &gt; Home &gt; Student
        </div>
    );
};

export default Header;
